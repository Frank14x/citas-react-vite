
const Error = ({mensaje}) => {
  return (
    <div className=" font-semibold bg-red-500 text-white text-center p-3 uppercase mb-3 rounded-md">
                  <p>{mensaje}</p>
                </div>

  )
}

export default Error
